import React from "react";
import TodoItem from "./TodoItem"

export default function TodoList(props) {
  const { list } = props;
  return(
    <div>
      {
        
      }
    </div>
  );
}